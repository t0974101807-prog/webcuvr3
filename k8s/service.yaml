apiVersion: v1
kind: Service
metadata:
  name: legal-os-service
  namespace: legal-os-prod
  labels:
    app: legal-os
    tier: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: legal-os
    tier: api
